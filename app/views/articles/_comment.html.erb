<div>
    <h4>Comment by <%= comment.author_name %></h4>
    <p class="comment">
        <% comment.body.split("\n").reject(&:empty?).each do |p|%>
            <%= p %> <br>
        <%end%>
    </p>
    <small><%= distance_of_time_in_words(Time.now, comment.created_at).capitalize %> ago</small>
</div>